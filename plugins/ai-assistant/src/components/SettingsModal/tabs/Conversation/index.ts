const name = 'Conversation';
const title = 'Conversation Settings';
const description =
  'Configure settings for your conversations with the AI Assistant.';

import { Tab } from './Conversation';

export default {
  name,
  title,
  description,
  Tab,
};
