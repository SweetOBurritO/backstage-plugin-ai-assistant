const name = 'Models';
const title = 'Model Selection';
const description =
  'Choose the chat model used by the AI Assistant for this conversation.';

import { Tab } from './Models';

export default {
  name,
  title,
  description,
  Tab,
};
